<% flash.each do |key, val| %>
    <div class="container">
      <div class="alert alert-<%= ApplicationHelper::typeOfAlertForKey(key) %>">
        <%= val %>
      </div>
    </div>
<% end unless flash.blank? %>